<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="tour">
	<insert id="insert" parameterType="list">
	    INSERT INTO tboard (b_title, b_content, b_img, b_url, b_theme, b_lat, b_lon, b_time) 
	    VALUES
	    <foreach collection="list" item="item" separator=",">
	        <!-- (#{item.b_title}, #{item.b_content}, #{item.b_img}, #{item.b_url}, #{item.b_theme}, #{item.b_lat}, #{item.b_lon}, #{item.b_time}) -->
	        (#{item.b_title}, #{item.b_content}, #{item.b_img}, #{item.b_url}, 1, #{item.b_lat}, #{item.b_lon}, #{item.b_time})	        
	    </foreach>
	</insert> 		
	
	<select id="list"  resultType="com.ict.project.vo.TboardVO">
    	select * from tboard  
  	</select>

    <select id="count" resultType="int">
    	<!-- select count(*) from tboard where b_like_active = 0 -->   
    	select count(*) from tboard 
    </select>
		
	<select id="listpage" parameterType="map" resultType="com.ict.project.vo.TboardVO">
     	select * from tboard order by b_idx asc limit #{limit} offset #{offset}      
    </select>

	<select id="listpageThema" parameterType="map" resultType="com.ict.project.vo.TboardVO">
     	select * from tboard where b_theme = #{b_theme} order by b_idx   asc limit #{limit} offset #{offset}      
    </select>

     <insert id="insertVO" parameterType="com.ict.project.vo.TboardVO">     
     	insert into tboard(b_title, b_content, b_loc,  b_lat,  b_lon,  b_img, b_theme, 
     					   b_time,  b_tel,     b_url,  b_like, b_hits, b_like_active)
     	values(#{b_title}, #{b_content}, #{b_loc},  #{b_lat},  #{b_lon},  #{b_img}, #{b_theme}, 
     		   #{b_time},  #{b_tel},     #{b_url},  0, 0, 0) 
     </insert>
     
     <select id="detail" parameterType="int" resultType="com.ict.project.vo.TboardVO">
     	select * from tboard where b_idx = #{b_idx}
     </select>

     <update id="update" parameterType="com.ict.project.vo.TboardVO">
		update tboard 
		set b_title = #{b_title}, b_content=#{b_content}, b_theme= #{b_theme}  		
		where b_idx = #{b_idx} 
     </update>
		
</mapper>
